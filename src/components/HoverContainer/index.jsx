import React from "react";
import "./styles.css"; // Importa o CSS

const HoverContainer = () => {
  return (
    <div className="hover-container">
      <p className="hover-content">Este é o texto que aparece!</p>
    </div>
  );
};

export default HoverContainer;
